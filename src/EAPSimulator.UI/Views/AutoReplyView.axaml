<UserControl x:Class="EAPSimulator.UI.Views.AutoReplyView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:EAPSimulator.UI.ViewModels"
             xmlns:controls="using:EAPSimulator.UI.Controls"
             xmlns:converters="using:EAPSimulator.UI.Converters"
             FontSize="12">

    <UserControl.Resources>
        <!-- Shared template so any FieldOption ComboBox shows DisplayName instead of the
             "EAPSimulator.UI.ViewModels.FieldOption" type name. -->
        <DataTemplate x:Key="FieldOptionTemplate" DataType="{x:Type vm:FieldOption}">
            <TextBlock Text="{Binding DisplayName}" FontSize="11"/>
        </DataTemplate>
    </UserControl.Resources>

    <UserControl.Styles>
        <!-- AutoCompleteBox 默认模板里的内层 TextBox 不响应 VerticalContentAlignment 属性；
             直接通过样式选择器把它强制垂直居中，免得文本贴顶。 -->
        <Style Selector="AutoCompleteBox /template/ TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <!-- 模板/消息名搜索框需要点击即能看到清单；MinimumPrefixLength=0 允许空文本时也打开列表。 -->
        <Style Selector="AutoCompleteBox.templatePicker">
            <Setter Property="MinimumPrefixLength" Value="0"/>
            <Setter Property="MinimumPopulateDelay" Value="0"/>
        </Style>
    </UserControl.Styles>

    <DockPanel>
        <!-- Top: Tab switcher (Quick Reply / Scenario) + Toolbar -->
        <Border DockPanel.Dock="Top" Padding="4,2" Background="#2D2D2D">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <RadioButton x:Name="QuickReplyTab" Content="快速回复" IsChecked="True"
                                 Click="OnQuickReplyTabClick" Margin="0,0,8,0" FontSize="11"/>
                    <RadioButton x:Name="ScenarioTab" Content="场景"
                                 Click="OnScenarioTabClick" FontSize="11"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Content="保存" Command="{Binding SaveRulesCommand}"
                            Classes="accent" Padding="8,2" FontSize="11" Margin="0,0,4,0"/>
                    <Button Content="加载" Command="{Binding LoadRulesCommand}"
                            Padding="8,2" FontSize="11" Margin="0,0,4,0"/>
                    <Button Content="📂" Click="OnBrowseFileClick"
                            Padding="5,1" FontSize="13" ToolTip.Tip="选择规则文件"/>
                </StackPanel>
                <TextBlock Text="{Binding StatusMessage}" FontSize="11" Opacity="0.6"
                           VerticalAlignment="Center" Margin="8,0,0,0"
                           TextTrimming="CharacterEllipsis"/>
            </DockPanel>
        </Border>

        <!-- Quick Reply Panel -->
        <Grid x:Name="QuickReplyPanel">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="120" MaxWidth="350"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Quick Reply rule list -->
            <DockPanel Grid.Column="0">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="4,2">
                    <Button Content="+ 添加" Command="{Binding AddQuickReplyCommand}"
                            Classes="accent" Padding="6,2" FontSize="11" Margin="0,0,4,0"/>
                    <Button Content="删除" Command="{Binding DeleteQuickReplyCommand}"
                            Padding="6,2" FontSize="11"/>
                </StackPanel>
                <ListBox ItemsSource="{Binding QuickReplies}"
                         SelectedItem="{Binding SelectedQuickReply}"
                         Padding="2"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayText}" FontSize="11" FontFamily="Consolas,monospace"
                                       TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <GridSplitter Grid.Column="1" Width="4"
                          HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

            <!-- Right: Quick Reply detail -->
            <Grid Grid.Column="2" Margin="8,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Empty state: shown when no rule selected -->
                <TextBlock x:Name="QuickReplyEmptyState"
                           Text="← 选择左侧规则或点击 '+ 添加' 创建新规则"
                           Opacity="0.4" FontSize="12"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="0,40,0,0"/>
                <!-- Content: shown when a rule is selected -->
                <ContentControl x:Name="QuickReplyDetailContent"
                                Content="{Binding SelectedQuickReply}"
                                HorizontalAlignment="Stretch">
                        <ContentControl.DataTemplates>
                            <DataTemplate DataType="{x:Type vm:QuickReplyRuleViewModel}">
                                <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Top">
                                    <!-- Enable + Description -->
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0" IsChecked="{Binding Enabled}" Content="启用"
                                                  VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <TextBlock Grid.Column="1" Text="描述:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Grid.Column="2" Text="{Binding Description}"
                                                 MinWidth="150" Padding="4,2" VerticalContentAlignment="Center"
                                                 HorizontalAlignment="Stretch"/>
                                    </Grid>

                                    <!-- Trigger (S,F) -->
                                    <Border Background="#252525" CornerRadius="4" Padding="8,6" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="触发消息" FontSize="11" FontWeight="SemiBold"
                                                       Foreground="#569CD6" Margin="0,0,0,6"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                <TextBlock Text="模板:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                <AutoCompleteBox Classes="templatePicker"
                                                                 ItemsSource="{Binding DataContext.TemplateNames, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Text="{Binding TriggerTemplateName}"
                                                                 FilterMode="Contains"
                                                                 Padding="4,2" FontSize="11" MinWidth="220"
                                                                 Watermark="(输入关键字,如 S6F11)"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Conditions — 紧跟触发消息，使匹配规则集中显示 -->
                                    <Border Background="#252525" CornerRadius="4" Padding="8,6" Margin="0,0,0,8">
                                        <StackPanel>
                                            <DockPanel Margin="0,0,0,6">
                                                <TextBlock Text="匹配条件 (可选，全部满足时才回复)" FontSize="11"
                                                           FontWeight="SemiBold" Foreground="#FF9800"
                                                           VerticalAlignment="Center" DockPanel.Dock="Left"/>
                                                <Button Content="+ 添加条件" Command="{Binding DataContext.AddConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        Padding="6,1" FontSize="11"
                                                        DockPanel.Dock="Right"/>
                                            </DockPanel>
                                            <ItemsControl ItemsSource="{Binding Conditions}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate DataType="{x:Type vm:FieldConditionViewModel}">
                                                        <StackPanel Orientation="Horizontal" Margin="0,2">
                                                            <!-- Legacy mode: field/path/op/value pickers -->
                                                            <StackPanel Orientation="Horizontal" IsVisible="{Binding IsLegacyMode}">
                                                                <TextBlock Text="字段:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                                <ComboBox ItemsSource="{Binding TemplateFields}"
                                                                          SelectedItem="{Binding SelectedFieldOption}"
                                                                          ItemTemplate="{StaticResource FieldOptionTemplate}"
                                                                          MinWidth="200" MaxWidth="320"
                                                                          Padding="3,1" FontSize="11"
                                                                          Margin="0,0,4,0"
                                                                          VerticalContentAlignment="Center"
                                                                          PlaceholderText="(选择字段)"/>
                                                                <ComboBox ItemsSource="{Binding Operators}"
                                                                          SelectedItem="{Binding Operator}"
                                                                          Width="55" Padding="3,1" FontSize="11"
                                                                          Margin="4,0" VerticalContentAlignment="Center"/>
                                                                <TextBlock Text="值:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                                <TextBox Text="{Binding Value}"
                                                                         Width="80" Padding="3,1" FontFamily="Microsoft YaHei" FontSize="11"
                                                                         Margin="0,0,4,0" VerticalContentAlignment="Center"/>
                                                            </StackPanel>
                                                            <!-- Expression mode: single textbox spanning the row -->
                                                            <StackPanel Orientation="Horizontal" IsVisible="{Binding IsExpressionMode}">
                                                                <TextBlock Text="表达式:" VerticalAlignment="Center" Margin="0,0,4,0"
                                                                           Foreground="#FFB454"/>
                                                                <TextBox Text="{Binding Expression}"
                                                                         MinWidth="380" Padding="3,1"
                                                                         FontFamily="Consolas,monospace" FontSize="11"
                                                                         Margin="0,0,4,0" VerticalContentAlignment="Center"
                                                                         ToolTip.Tip="支持 secs[&quot;0/1&quot;] / vars[&quot;name&quot;] / host[&quot;field&quot;] / num()/contains() 等"/>
                                                            </StackPanel>
                                                            <!-- Mode toggle: clearing Expression returns to legacy mode -->
                                                            <Button Content="ƒx" Click="OnToggleExpressionMode"
                                                                    Padding="6,0" FontSize="11" Margin="0,0,4,0"
                                                                    ToolTip.Tip="切换 表达式 / 字段-操作符-值 两种模式"/>
                                                            <Button Content="✕" Command="{Binding DataContext.DeleteConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"
                                                                    Padding="4,0" FontSize="11"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>

                                    <!-- Reply Template Picker -->
                                    <Border Background="#252525" CornerRadius="4" Padding="8,6" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="回复模板 (从消息编辑器中选择)" FontSize="11"
                                                       FontWeight="SemiBold" Foreground="#4CAF50" Margin="0,0,0,6"/>
                                            <AutoCompleteBox Classes="templatePicker"
                                                             ItemsSource="{Binding DataContext.TemplateNames, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                             Text="{Binding ReplyTemplateName}"
                                                             FilterMode="Contains"
                                                             Padding="4,2" FontSize="12"
                                                             Watermark="(输入关键字过滤回复模板)"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </ContentControl.DataTemplates>
                    </ContentControl>
            </Grid>
        </Grid>

        <!-- Scenario Panel (hidden by default) — Famate-style 3 columns -->
        <Grid x:Name="ScenarioPanel" IsVisible="False">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Run-control bar -->
            <Border Grid.Row="0" Background="#252525" Padding="8,4" Margin="0,0,0,2">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <Button Content="▶ 运行" Command="{Binding RunScenarioCommand}"
                                Classes="accent" Padding="8,2" FontSize="11" Margin="0,0,4,0"
                                ToolTip.Tip="运行选中的场景(需要先连接)"/>
                        <Button Content="⏹ 停止" Command="{Binding StopScenarioCommand}"
                                Padding="8,2" FontSize="11" Margin="0,0,8,0"/>
                        <!-- Debugger controls — only relevant while a scenario is running. -->
                        <Button Content="⏸ 暂停" Command="{Binding PauseScenarioCommand}"
                                Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                ToolTip.Tip="在下一个步骤边界暂停"/>
                        <Button Content="▶▶ 继续" Command="{Binding ContinueScenarioCommand}"
                                Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                ToolTip.Tip="从暂停恢复执行直到下一个断点或结束"/>
                        <Button Content="⏭ 单步" Command="{Binding StepOverScenarioCommand}"
                                Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                ToolTip.Tip="执行一个步骤后再次暂停"/>
                        <Button Content="🔴 断点" Command="{Binding ToggleBreakpointCommand}"
                                Padding="8,2" FontSize="11" Margin="0,0,12,0"
                                ToolTip.Tip="在选中步骤上切换断点"/>
                        <TextBlock Text="状态:" VerticalAlignment="Center" Opacity="0.6" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding RunStatus}" VerticalAlignment="Center"
                                   Foreground="#4CAF50" FontFamily="Consolas,monospace" FontSize="11"/>
                        <TextBlock Text=" ⏸" VerticalAlignment="Center"
                                   Foreground="#FFB454" FontSize="13" Margin="6,0,0,0"
                                   IsVisible="{Binding IsPaused}"
                                   ToolTip.Tip="已暂停"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" MinWidth="160" MaxWidth="400"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" MinWidth="240"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="360" MinWidth="260" MaxWidth="600"/>
                </Grid.ColumnDefinitions>

                <!-- Column 0: Scenario list -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="4,2">
                        <Button Content="+ 场景" Command="{Binding AddScenarioCommand}"
                                Classes="accent" Padding="6,2" FontSize="11" Margin="0,0,4,0"/>
                        <Button Content="删除" Command="{Binding DeleteScenarioCommand}"
                                Padding="6,2" FontSize="11"/>
                    </StackPanel>
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding Scenarios}"
                             SelectedItem="{Binding SelectedScenario}"
                             Padding="2"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Border Background="#1F1F1F" CornerRadius="2"
                                            Padding="4,1" Margin="0,0,6,0">
                                        <TextBlock Text="{Binding RoleBadge}"
                                                   Foreground="{Binding RoleBadgeColor}"
                                                   FontSize="9" FontWeight="Bold"/>
                                    </Border>
                                    <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="SemiBold"/>
                                    <TextBlock Text=" (" FontSize="11" Opacity="0.5"/>
                                    <TextBlock Text="{Binding Steps.Count}" FontSize="11"/>
                                    <TextBlock Text=" 步)" FontSize="11" Opacity="0.5"/>
                                    <!-- 触发配置异常时的小警告标记，详情在右侧详情面板。 -->
                                    <TextBlock Text=" ⚠" FontSize="11" Foreground="#FFB454"
                                               IsVisible="{Binding HasTriggerWarning}"
                                               ToolTip.Tip="{Binding TriggerWarning}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>

                <GridSplitter Grid.Column="1" Width="4"
                              HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

                <!-- Column 2: Step sequence -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Vertical" Margin="4,2">
                        <!-- Row 1: 节点操作 + 视图切换 -->
                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="操作:" FontSize="11" FontWeight="SemiBold"
                                       VerticalAlignment="Center" Margin="0,0,8,2"/>
                            <Button Content="删除" Command="{Binding DeleteStepCommand}"
                                    Padding="6,2" FontSize="11" Margin="0,0,3,2"/>
                            <Button Content="↑" Command="{Binding MoveStepUpCommand}"
                                    Padding="6,2" FontSize="11" Margin="0,0,2,2"
                                    ToolTip.Tip="上移"/>
                            <Button Content="↓" Command="{Binding MoveStepDownCommand}"
                                    Padding="6,2" FontSize="11" Margin="0,0,12,2"
                                    ToolTip.Tip="下移"/>
                            <!-- View mode toggle: flow canvas vs legacy list -->
                            <Button Content="📋 列表" Click="OnToggleListViewClick"
                                    Padding="6,2" FontSize="11" Margin="0,0,2,2"
                                    ToolTip.Tip="切换到列表视图(键盘导航/批量编辑友好)"/>
                            <Button Content="🌐 流程图" Click="OnToggleFlowViewClick"
                                    Padding="6,2" FontSize="11" Margin="0,0,2,2"
                                    ToolTip.Tip="切换到流程图视图(可视化拖动节点)"/>
                            <Button Content="↺ 重置布局" Click="OnResetLayoutClick"
                                    Padding="6,2" FontSize="11" Margin="0,0,0,2"
                                    ToolTip.Tip="把流程图节点位置恢复到默认列布局"/>
                        </WrapPanel>
                        <!-- Row 2: 添加步骤 -->
                        <WrapPanel Orientation="Horizontal">
                            <TextBlock Text="步骤:" FontSize="11" FontWeight="SemiBold"
                                       VerticalAlignment="Center" Margin="0,0,8,2"/>
                            <Button Content="▶ Send" Command="{Binding AddStepCommand}" CommandParameter="Send"
                                    Background="#1E4D2B" Foreground="#A6E3A1"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 Send 步骤 — 主动发送一条模板消息"/>
                            <Button Content="◀ Recv" Command="{Binding AddStepCommand}" CommandParameter="Receive"
                                    Background="#1E3A5F" Foreground="#89B4FA"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 Receive 步骤 — 等待对端消息(可设超时)"/>
                            <Button Content="↩ Reply" Command="{Binding AddStepCommand}" CommandParameter="Reply"
                                    Background="#3D2B5C" Foreground="#CBA6F7"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 Reply 步骤 — 回复上一条接收到的消息"/>
                            <Button Content="⏱ Delay" Command="{Binding AddStepCommand}" CommandParameter="Delay"
                                    Background="#5C4326" Foreground="#FAB387"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 Delay 步骤 — 等待指定毫秒数"/>
                            <Button Content="📝 Log" Command="{Binding AddStepCommand}" CommandParameter="Log"
                                    Background="#3A3A3A" Foreground="#CDD6F4"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 Log 步骤 — 输出一条日志/状态文本"/>
                            <Button Content="⑂ Branch" Command="{Binding AddStepCommand}" CommandParameter="Branch"
                                    Background="#5C2B3D" Foreground="#F38BA8"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 Branch 步骤 — 按上一条接收消息的字段值跳转到不同 Label"/>
                            <Button Content="𝑥 SetVar" Command="{Binding AddStepCommand}" CommandParameter="SetVariable"
                                    Background="#3A4A2C" Foreground="#A6E3A1"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 SetVariable 步骤 — 给变量赋值，可用 ${name} 在模板中引用"/>
                            <Button Content="⟳ Loop" Command="{Binding AddStepCommand}" CommandParameter="Loop"
                                    Background="#2C3E5C" Foreground="#74C7EC"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 Loop 步骤 — 与同 ID 的 EndLoop 配对，重复执行中间步骤"/>
                            <Button Content="⤴ EndLoop" Command="{Binding AddStepCommand}" CommandParameter="EndLoop"
                                    Background="#2C3E5C" Foreground="#74C7EC"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 EndLoop 步骤 — 关闭最近的 Loop"/>
                            <Button Content="⟳⃗ ForEach" Command="{Binding AddStepCommand}" CommandParameter="ForEach"
                                    Background="#2C5C4A" Foreground="#94E2D5"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 ForEach 步骤 — 遍历列表(SECS子项/Host数组/变量分隔)"/>
                            <Button Content="⤴ EndForEach" Command="{Binding AddStepCommand}" CommandParameter="EndForEach"
                                    Background="#2C5C4A" Foreground="#94E2D5"
                                    Padding="8,2" FontSize="11" Margin="0,0,3,2"
                                    ToolTip.Tip="添加 EndForEach 步骤 — 关闭最近的 ForEach"/>
                            <Button Content="⏎ Call" Command="{Binding AddStepCommand}" CommandParameter="CallScenario"
                                    Background="#4A2C5C" Foreground="#CBA6F7"
                                    Padding="8,2" FontSize="11" Margin="0,0,0,2"
                                    ToolTip.Tip="添加 CallScenario 步骤 — 调用另一个场景作为子例程"/>
                        </WrapPanel>
                    </StackPanel>
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding SelectedScenario.Steps}"
                             SelectedItem="{Binding SelectedStep}"
                             IsVisible="{Binding IsListView}"
                             Padding="2">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Converter={x:Static converters:ListBoxIndexConverter.Instance}}"
                                               FontSize="10" Opacity="0.5" Margin="0,0,6,0" MinWidth="20"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding DisplayText}" FontSize="11"
                                               FontFamily="Consolas,monospace"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <controls:ScenarioFlowCanvas Grid.Row="1"
                                                 x:Name="FlowCanvas"
                                                 Scenario="{Binding SelectedScenario}"
                                                 SelectedStep="{Binding SelectedStep, Mode=TwoWay}"
                                                 PausedStepIndex="{Binding PausedStepIndex}"
                                                 IsVisible="{Binding IsFlowView}"/>
                </Grid>

                <GridSplitter Grid.Column="3" Width="4"
                              HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

                <!-- Column 4: Detail editor (Scenario settings + Step editor by Kind) -->
                <ScrollViewer Grid.Column="4" Padding="8,4" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <!-- Scenario settings -->
                        <Border x:Name="ScenarioSettingsBorder" Background="#252525"
                                CornerRadius="4" Padding="8,6" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="场景设置" FontSize="11" FontWeight="SemiBold"
                                           Foreground="#569CD6" Margin="0,0,0,6"/>
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="名称:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" Text="{Binding SelectedScenario.Name}"
                                             Padding="4,2" VerticalContentAlignment="Center"/>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="描述:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" Text="{Binding SelectedScenario.Description}"
                                             Padding="4,2" VerticalContentAlignment="Center"/>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="角色:" VerticalAlignment="Center"
                                               ToolTip.Tip="此场景为哪一侧编写。Host=EAP/Active, Equipment=设备/Passive, Any=两侧通用。运行时会校验。"/>
                                    <ComboBox Grid.Column="1"
                                              ItemsSource="{Binding SelectedScenario.RoleNames}"
                                              SelectedItem="{Binding SelectedScenario.RoleName}"
                                              Padding="4,2" FontSize="11"/>
                                </Grid>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding SelectedScenario.Enabled}"
                                              Content="启用" Margin="0,0,12,0"/>
                                    <CheckBox IsChecked="{Binding SelectedScenario.Loop}"
                                              Content="循环" Margin="0,0,12,0"/>
                                    <CheckBox IsChecked="{Binding SelectedScenario.AutoStart}"
                                              Content="连接后自动启动"/>
                                </StackPanel>

                                <!-- 消息触发 — 引擎空闲时收到匹配的 (S,F)+条件 的入站消息，自动 Start 本场景 -->
                                <Border BorderBrush="#404040" BorderThickness="0,1,0,0"
                                        Margin="0,8,0,0" Padding="0,6,0,0">
                                    <StackPanel>
                                        <CheckBox IsChecked="{Binding SelectedScenario.TriggerOnMessage}"
                                                  Content="按入站消息触发 (空闲时收到匹配消息自动启动)"
                                                  ToolTip.Tip="典型场景：S6F11 + CEID==某值。&#x0a;只在引擎空闲时生效；正在运行的场景不会再次触发。"/>
                                        <!-- 静态校验提示：勾了触发但首步不是 Receive 之类配置坑。 -->
                                        <Border Background="#3D2B1F" BorderBrush="#FF9800" BorderThickness="1"
                                                CornerRadius="3" Padding="6,3" Margin="0,4,0,0"
                                                IsVisible="{Binding SelectedScenario.HasTriggerWarning}">
                                            <TextBlock Text="{Binding SelectedScenario.TriggerWarning}"
                                                       Foreground="#FFB454" FontSize="11"
                                                       TextWrapping="Wrap"/>
                                        </Border>
                                        <StackPanel IsEnabled="{Binding SelectedScenario.TriggerOnMessage}">
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                                                <TextBlock Text="模板搜索:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                <AutoCompleteBox Classes="templatePicker"
                                                                 ItemsSource="{Binding TemplateNames}"
                                                                 Text="{Binding SelectedScenario.TriggerTemplateName}"
                                                                 FilterMode="Contains"
                                                                 MinWidth="220" Padding="4,2" FontSize="11"
                                                                 Watermark="输入 S6 / CEID 名称等自动过滤"/>
                                                <Button Content="+ 条件"
                                                        Command="{Binding AddTriggerConditionCommand}"
                                                        Padding="6,1" FontSize="10" Margin="8,0,0,0"/>
                                            </StackPanel>
                                            <ItemsControl ItemsSource="{Binding SelectedScenario.TriggerConditions}"
                                                          Margin="0,4,0,0">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate DataType="{x:Type vm:FieldConditionViewModel}">
                                                        <StackPanel Orientation="Horizontal" Margin="0,2">
                                                            <StackPanel Orientation="Horizontal" IsVisible="{Binding IsLegacyMode}">
                                                                <ComboBox ItemsSource="{Binding TemplateFields}"
                                                                          SelectedItem="{Binding SelectedFieldOption}"
                                                                          ItemTemplate="{StaticResource FieldOptionTemplate}"
                                                                          MinWidth="180" MaxWidth="260"
                                                                          Padding="3,1" FontSize="11" Margin="0,0,4,0"
                                                                          VerticalContentAlignment="Center"
                                                                          PlaceholderText="(选择字段)"/>
                                                                <ComboBox ItemsSource="{Binding Operators}"
                                                                          SelectedItem="{Binding Operator}"
                                                                          Width="55" Padding="3,1" FontSize="11"
                                                                          Margin="4,0" VerticalContentAlignment="Center"/>
                                                                <TextBox Text="{Binding Value}"
                                                                         Width="90" Padding="3,1" FontSize="11"
                                                                         Margin="0,0,4,0" VerticalContentAlignment="Center"
                                                                         ToolTip.Tip="期望值,例 1001"/>
                                                            </StackPanel>
                                                            <StackPanel Orientation="Horizontal" IsVisible="{Binding IsExpressionMode}">
                                                                <TextBlock Text="ƒx:" VerticalAlignment="Center" Margin="0,0,4,0"
                                                                           Foreground="#FFB454"/>
                                                                <TextBox Text="{Binding Expression}"
                                                                         MinWidth="220" Padding="3,1"
                                                                         FontFamily="Consolas,monospace" FontSize="11"
                                                                         Margin="0,0,4,0" VerticalContentAlignment="Center"/>
                                                            </StackPanel>
                                                            <Button Content="ƒx" Click="OnToggleExpressionMode"
                                                                    Padding="6,0" FontSize="11" Margin="0,0,4,0"
                                                                    ToolTip.Tip="切换 表达式 / 字段-操作符-值"/>
                                                            <Button Content="✕"
                                                                    Command="{Binding DataContext.DeleteTriggerConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"
                                                                    Padding="4,0" FontSize="11"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- Step editor — visibility per Kind -->
                        <Border x:Name="StepDetailBorder" Background="#252525"
                                CornerRadius="4" Padding="8,6" Margin="0,0,0,8">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,8">
                                    <TextBlock Text="步骤详情" FontSize="11" FontWeight="SemiBold"
                                               Foreground="#4CAF50" VerticalAlignment="Center"
                                               DockPanel.Dock="Left"/>
                                    <ComboBox ItemsSource="{Binding SelectedStep.KindNames}"
                                              SelectedItem="{Binding SelectedStep.KindName}"
                                              Padding="4,2" FontSize="11" Margin="8,0,0,0"
                                              ToolTip.Tip="改变步骤类型"/>
                                </DockPanel>

                                <!-- Label (always shown) -->
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="备注:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" Text="{Binding SelectedStep.Label}"
                                             Padding="4,2" VerticalContentAlignment="Center"
                                             Watermark="可选,显示在步骤名后"/>
                                </Grid>

                                <!-- OnError label (always shown) — routes exceptions/timeouts to a step by label. -->
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="出错跳转:" VerticalAlignment="Center"
                                               Foreground="#F38BA8"
                                               ToolTip.Tip="本步骤抛异常/超时(Fail) 时跳到指定 Label;留空 = 失败终止场景"/>
                                    <AutoCompleteBox Grid.Column="1"
                                                     ItemsSource="{Binding SelectedStep.AvailableLabels}"
                                                     Text="{Binding SelectedStep.OnErrorLabel}"
                                                     Padding="4,2" FontSize="11"
                                                     Watermark="(可选,选已有 Label 或输入新名)"/>
                                </Grid>

                                <!-- Send / Reply: template picker -->
                                <StackPanel IsVisible="{Binding SelectedStep.UsesTemplate}">
                                    <TextBlock Text="发送模板:" FontSize="11" Foreground="#569CD6" Margin="0,2,0,4"/>
                                    <AutoCompleteBox Classes="templatePicker"
                                                     ItemsSource="{Binding TemplateNames}"
                                                     Text="{Binding SelectedStep.TemplateName}"
                                                     FilterMode="Contains"
                                                     Padding="4,2" FontSize="11"
                                                     Watermark="(输入关键字过滤模板,如 S6)"/>
                                </StackPanel>

                                <!-- Send extras -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsSendKind}" Margin="0,4,0,0">
                                    <CheckBox IsChecked="{Binding SelectedStep.WaitReply}"
                                              Content="等待对方 W-bit 回复" FontSize="11"/>
                                </StackPanel>

                                <!-- Receive: 模板提示自动填充 S/F + conditions + timeout -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsReceiveKind}">
                                    <TextBlock Text="期望接收:" FontSize="11" Foreground="#569CD6" Margin="0,4,0,4"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="模板提示:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <AutoCompleteBox Classes="templatePicker"
                                                         ItemsSource="{Binding TemplateNames}"
                                                         Text="{Binding SelectedStep.TemplateName}"
                                                         FilterMode="Contains"
                                                         Padding="4,2" FontSize="11" MinWidth="220"
                                                         Watermark="(输入关键字,如 S6F11；留空匹配任意消息)"/>
                                    </StackPanel>

                                    <DockPanel Margin="0,4,0,4">
                                        <TextBlock Text="条件:" FontSize="11" Foreground="#FF9800"
                                                   VerticalAlignment="Center" DockPanel.Dock="Left"/>
                                        <Button Content="+ 条件"
                                                Command="{Binding AddConditionCommand}"
                                                Padding="6,1" FontSize="10"
                                                DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <ItemsControl ItemsSource="{Binding SelectedStep.Conditions}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:FieldConditionViewModel}">
                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <StackPanel Orientation="Horizontal" IsVisible="{Binding IsLegacyMode}">
                                                        <ComboBox ItemsSource="{Binding TemplateFields}"
                                                                  SelectedItem="{Binding SelectedFieldOption}"
                                                                  ItemTemplate="{StaticResource FieldOptionTemplate}"
                                                                  MinWidth="180" MaxWidth="260"
                                                                  Padding="3,1" FontSize="11" Margin="0,0,4,0"
                                                                  VerticalContentAlignment="Center"
                                                                  PlaceholderText="(选择字段)"/>
                                                        <ComboBox ItemsSource="{Binding Operators}"
                                                                  SelectedItem="{Binding Operator}"
                                                                  Width="55" Padding="3,1" FontSize="11"
                                                                  Margin="4,0" VerticalContentAlignment="Center"/>
                                                        <TextBox Text="{Binding Value}"
                                                                 Width="70" Padding="3,1" FontSize="11"
                                                                 Margin="0,0,4,0" VerticalContentAlignment="Center"/>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Horizontal" IsVisible="{Binding IsExpressionMode}">
                                                        <TextBlock Text="ƒx:" VerticalAlignment="Center" Margin="0,0,4,0"
                                                                   Foreground="#FFB454"/>
                                                        <TextBox Text="{Binding Expression}"
                                                                 MinWidth="320" Padding="3,1"
                                                                 FontFamily="Consolas,monospace" FontSize="11"
                                                                 Margin="0,0,4,0" VerticalContentAlignment="Center"
                                                                 ToolTip.Tip="secs[&quot;0/1&quot;] / vars[&quot;x&quot;] / host[&quot;f&quot;] / num()/contains()"/>
                                                    </StackPanel>
                                                    <Button Content="ƒx" Click="OnToggleExpressionMode"
                                                            Padding="6,0" FontSize="11" Margin="0,0,4,0"
                                                            ToolTip.Tip="切换 表达式 / 字段-操作符-值 两种模式"/>
                                                    <Button Content="✕"
                                                            Command="{Binding DataContext.DeleteConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            Padding="4,0" FontSize="11"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                        <TextBlock Text="超时(ms):" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.TimeoutMs}"
                                                 Width="80" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"/>
                                        <TextBlock Text="超时时:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                                        <ComboBox ItemsSource="{Binding SelectedStep.OnTimeoutNames}"
                                                  SelectedItem="{Binding SelectedStep.OnTimeoutName}"
                                                  Padding="4,2" FontSize="11"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Delay -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsDelayKind}" Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="延时(ms):" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBox Text="{Binding SelectedStep.DelayMs}"
                                             Width="100" Padding="4,2"
                                             FontFamily="Consolas,monospace"
                                             VerticalContentAlignment="Center"/>
                                </StackPanel>

                                <!-- Log -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsLogKind}" Margin="0,4,0,0">
                                    <TextBlock Text="日志内容:" FontSize="11" Foreground="#569CD6" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding SelectedStep.Message}"
                                             Padding="4,2" VerticalContentAlignment="Center"
                                             Watermark="将输出到状态栏与日志"/>
                                </StackPanel>

                                <!-- Branch -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsBranchKind}" Margin="0,4,0,0">
                                    <TextBlock Text="按上一条接收消息分支跳转:" FontSize="11"
                                               Foreground="#F38BA8" Margin="0,0,0,4"/>
                                    <TextBlock Text="提示: 条件为 AND 关系,首个匹配的 case 跳到对应 Label;无匹配走默认 Label,默认空则继续往下。"
                                               FontSize="10" Opacity="0.55" TextWrapping="Wrap" Margin="0,0,0,6"/>

                                    <DockPanel Margin="0,0,0,4">
                                        <TextBlock Text="Cases:" FontSize="11" Foreground="#FF9800"
                                                   VerticalAlignment="Center" DockPanel.Dock="Left"/>
                                        <Button Content="+ Case" Command="{Binding AddBranchCaseCommand}"
                                                Padding="6,1" FontSize="10"
                                                DockPanel.Dock="Right"/>
                                    </DockPanel>

                                    <ItemsControl ItemsSource="{Binding SelectedStep.Cases}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:BranchCaseViewModel}">
                                                <Border Background="#1F1F1F" CornerRadius="3"
                                                        Padding="6,4" Margin="0,2"
                                                        BorderBrush="#444" BorderThickness="1">
                                                    <StackPanel>
                                                        <DockPanel Margin="0,0,0,4">
                                                            <Button Content="✕"
                                                                    Command="{Binding DataContext.DeleteBranchCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"
                                                                    Padding="4,0" FontSize="11"
                                                                    DockPanel.Dock="Right"
                                                                    ToolTip.Tip="删除此 case"/>
                                                            <Button Content="+ 条件"
                                                                    Command="{Binding DataContext.AddBranchCaseConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"
                                                                    Padding="6,1" FontSize="10"
                                                                    DockPanel.Dock="Right" Margin="0,0,4,0"/>
                                                            <TextBlock Text="跳到 Label:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                            <AutoCompleteBox ItemsSource="{Binding DataContext.SelectedStep.AvailableLabels, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Text="{Binding TargetLabel}"
                                                                             MinWidth="160" Padding="4,2" FontSize="11"
                                                                             Margin="0,0,8,0"
                                                                             Watermark="(选择/输入)"/>
                                                        </DockPanel>
                                                        <ItemsControl ItemsSource="{Binding Conditions}">
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate DataType="{x:Type vm:FieldConditionViewModel}">
                                                                    <StackPanel Orientation="Horizontal" Margin="0,2">
                                                                        <StackPanel Orientation="Horizontal" IsVisible="{Binding IsLegacyMode}">
                                                                            <ComboBox ItemsSource="{Binding TemplateFields}"
                                                                                      SelectedItem="{Binding SelectedFieldOption}"
                                                                                      ItemTemplate="{StaticResource FieldOptionTemplate}"
                                                                                      MinWidth="160" MaxWidth="240"
                                                                                      Padding="3,1" FontSize="11" Margin="0,0,4,0"
                                                                                      VerticalContentAlignment="Center"
                                                                                      PlaceholderText="(选择字段)"/>
                                                                            <ComboBox ItemsSource="{Binding Operators}"
                                                                                      SelectedItem="{Binding Operator}"
                                                                                      Width="55" Padding="3,1" FontSize="11"
                                                                                      Margin="4,0" VerticalContentAlignment="Center"/>
                                                                            <TextBox Text="{Binding Value}"
                                                                                     Width="70" Padding="3,1" FontSize="11"
                                                                                     Margin="0,0,4,0" VerticalContentAlignment="Center"/>
                                                                        </StackPanel>
                                                                        <StackPanel Orientation="Horizontal" IsVisible="{Binding IsExpressionMode}">
                                                                            <TextBlock Text="ƒx:" VerticalAlignment="Center" Margin="0,0,4,0"
                                                                                       Foreground="#FFB454"/>
                                                                            <TextBox Text="{Binding Expression}"
                                                                                     MinWidth="280" Padding="3,1"
                                                                                     FontFamily="Consolas,monospace" FontSize="11"
                                                                                     Margin="0,0,4,0" VerticalContentAlignment="Center"
                                                                                     ToolTip.Tip="secs[&quot;0/1&quot;] / vars[&quot;x&quot;] / host[&quot;f&quot;] / num()/contains()"/>
                                                                        </StackPanel>
                                                                        <Button Content="ƒx" Click="OnToggleExpressionMode"
                                                                                Padding="6,0" FontSize="11" Margin="0,0,4,0"
                                                                                ToolTip.Tip="切换 表达式 / 字段-操作符-值 两种模式"/>
                                                                        <Button Content="✕"
                                                                                Command="{Binding DataContext.DeleteBranchCaseConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                CommandParameter="{Binding}"
                                                                                Padding="4,0" FontSize="11"/>
                                                                    </StackPanel>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="默认(无匹配时跳到):" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <AutoCompleteBox ItemsSource="{Binding SelectedStep.AvailableLabels}"
                                                         Text="{Binding SelectedStep.DefaultLabel}"
                                                         MinWidth="160" Padding="4,2" FontSize="11"
                                                         Watermark="(留空 = 继续往下)"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- HostSend: pick a host template by name -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsHostSendKind}" Margin="0,4,0,0">
                                    <TextBlock Text="Host 发送 (EAP→MES/RMS):" FontSize="11"
                                               Foreground="#FFB454" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="消息名:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <AutoCompleteBox Classes="templatePicker"
                                                         ItemsSource="{Binding HostMessageNames}"
                                                         Text="{Binding SelectedStep.HostMessageName}"
                                                         FilterMode="Contains"
                                                         Padding="4,2" FontSize="11" MinWidth="240"
                                                         Watermark="(输入关键字过滤)"/>
                                    </StackPanel>
                                    <TextBlock Text="提示: Host 消息模板从 host_message_templates.json 加载；字段值在模板里配置，支持在模板默认值/RawBody 中使用 ${变量名}。"
                                               FontSize="10" Opacity="0.55" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- HostReceive: name match + conditions + timeout -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsHostReceiveKind}" Margin="0,4,0,0" Tag="HostReceiveConditions">
                                    <TextBlock Text="Host 接收 (MES/RMS→EAP):" FontSize="11"
                                               Foreground="#FFB454" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="消息名:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <AutoCompleteBox Classes="templatePicker"
                                                         ItemsSource="{Binding HostMessageNames}"
                                                         Text="{Binding SelectedStep.HostMessageName}"
                                                         FilterMode="Contains"
                                                         Padding="4,2" FontSize="11" MinWidth="240"
                                                         Watermark="(留空匹配任意 Host 消息)"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="超时(ms):" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.TimeoutMs}"
                                                 Width="100" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"/>
                                        <TextBlock Text="超时动作:" VerticalAlignment="Center"
                                                   Margin="12,0,6,0"/>
                                        <ComboBox ItemsSource="{Binding SelectedStep.OnTimeoutNames}"
                                                  SelectedItem="{Binding SelectedStep.OnTimeoutName}"
                                                  Padding="4,2" FontSize="11"/>
                                    </StackPanel>
                                    <TextBlock Text="提示: 可直接在此步骤添加 MES 返回字段条件；Path 写 Host 字段名（如 typeId / insResult），表达式可写 host[&quot;typeId&quot;] == &quot;O&quot;。"
                                               FontSize="10" Opacity="0.55" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                    <DockPanel Margin="0,6,0,4">
                                        <TextBlock Text="条件:" FontSize="11" Foreground="#FF9800"
                                                   VerticalAlignment="Center" DockPanel.Dock="Left"/>
                                        <Button Content="+ 条件"
                                                Command="{Binding AddConditionCommand}"
                                                Padding="6,1" FontSize="10"
                                                DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <ItemsControl ItemsSource="{Binding SelectedStep.Conditions}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:FieldConditionViewModel}">
                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <StackPanel Orientation="Horizontal" IsVisible="{Binding IsLegacyMode}">
                                                        <TextBlock Text="字段:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                        <TextBox Text="{Binding Path}"
                                                                 Width="140" Padding="3,1" FontSize="11"
                                                                 Margin="0,0,4,0" VerticalContentAlignment="Center"
                                                                 Watermark="Host字段名"/>
                                                        <ComboBox ItemsSource="{Binding Operators}"
                                                                  SelectedItem="{Binding Operator}"
                                                                  Width="55" Padding="3,1" FontSize="11"
                                                                  Margin="4,0" VerticalContentAlignment="Center"/>
                                                        <TextBox Text="{Binding Value}"
                                                                 Width="90" Padding="3,1" FontSize="11"
                                                                 Margin="0,0,4,0" VerticalContentAlignment="Center"
                                                                 Watermark="期望值"/>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Horizontal" IsVisible="{Binding IsExpressionMode}">
                                                        <TextBlock Text="ƒx:" VerticalAlignment="Center" Margin="0,0,4,0"
                                                                   Foreground="#FFB454"/>
                                                        <TextBox Text="{Binding Expression}"
                                                                 MinWidth="260" Padding="3,1"
                                                                 FontFamily="Consolas,monospace" FontSize="11"
                                                                 Margin="0,0,4,0" VerticalContentAlignment="Center"
                                                                 ToolTip.Tip="host[&quot;typeId&quot;] / host.Name / vars[&quot;x&quot;] / contains()"/>
                                                    </StackPanel>
                                                    <Button Content="ƒx" Click="OnToggleExpressionMode"
                                                            Padding="6,0" FontSize="11" Margin="0,0,4,0"
                                                            ToolTip.Tip="切换 表达式 / 字段-操作符-值 两种模式"/>
                                                    <Button Content="✕"
                                                            Command="{Binding DataContext.DeleteConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            Padding="4,0" FontSize="11"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- SetVariable: assign a literal or captured field value to a variable -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsSetVariableKind}" Margin="0,4,0,0">
                                    <TextBlock Text="设置变量:" FontSize="11"
                                               Foreground="#A6E3A1" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="变量名:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.VariableName}"
                                                 Width="160" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"/>
                                        <TextBlock Text="来源:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                                        <ComboBox ItemsSource="{Binding SelectedStep.VariableSourceNames}"
                                                  SelectedItem="{Binding SelectedStep.VariableSourceName}"
                                                  Padding="4,2" FontSize="11" MinWidth="120"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="字面量:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.LiteralValue}"
                                                 Width="280" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="支持嵌套 ${var} 引用其他变量"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="字段路径:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.VariablePath}"
                                                 Width="200" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="LastSecsField 用 0/1/2;LastHostField 用字段名"/>
                                    </StackPanel>
                                    <TextBlock Text="提示: 之后在 Send/Reply/HostSend 模板里用 ${变量名} 引用。"
                                               FontSize="10" Opacity="0.55" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- Loop: start of a loop body -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsLoopKind}" Margin="0,4,0,0">
                                    <TextBlock Text="循环开始:" FontSize="11"
                                               Foreground="#74C7EC" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="LoopId:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.LoopId}"
                                                 Width="100" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="与配对的 EndLoop 用同一个 ID"/>
                                        <TextBlock Text="次数:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.LoopTimes}"
                                                 Width="80" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="0 = 无限循环或交给 While 表达式控制"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="While ƒx:" VerticalAlignment="Center" Margin="0,0,6,0"
                                                   Foreground="#FFB454"/>
                                        <TextBox Text="{Binding SelectedStep.LoopWhile}"
                                                 MinWidth="320" Padding="4,2"
                                                 FontFamily="Consolas,monospace" FontSize="11"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="布尔表达式;为 false 时退出循环。如 num(vars[&quot;i&quot;]) &lt; 10"/>
                                    </StackPanel>
                                    <TextBlock Text="提示: 当前轮次写入 ${$loop.&lt;LoopId&gt;.i} 和 loop[&quot;&lt;LoopId&gt;&quot;];次数 &gt; 0 优先,否则用 While 表达式。"
                                               FontSize="10" Opacity="0.55" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- EndLoop: close a loop body -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsEndLoopKind}" Margin="0,4,0,0">
                                    <TextBlock Text="循环结束:" FontSize="11"
                                               Foreground="#74C7EC" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="LoopId:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.LoopId}"
                                                 Width="100" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="必须与匹配的 Loop 步骤相同"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- ForEach: start of a foreach body -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsForEachKind}" Margin="0,4,0,0">
                                    <TextBlock Text="遍历开始:" FontSize="11"
                                               Foreground="#94E2D5" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="ForEachId:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.ForEachId}"
                                                 Width="100" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="与配对的 EndForEach 用同一个 ID"/>
                                        <TextBlock Text="来源:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                                        <ComboBox ItemsSource="{Binding SelectedStep.ForEachSourceNames}"
                                                  SelectedItem="{Binding SelectedStep.ForEachSourceName}"
                                                  Padding="4,2" FontSize="11" MinWidth="140"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="路径/字段/变量名:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.ForEachPath}"
                                                 Width="220" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="SecsList: 路径 (如 1/0,空=根);HostArrayList: 字段名;Variable: 变量名(无 ${})"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="别名变量(可选):" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.ForEachItemVariable}"
                                                 Width="120" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="非空时把当前项额外写到 ${名字} 便于模板引用"/>
                                        <TextBlock Text="分隔符:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.ForEachSeparator}"
                                                 Width="60" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="仅 Variable 来源使用,默认 ,"/>
                                    </StackPanel>
                                    <TextBlock Text="提示: 当前项写入 ${$foreach.&lt;Id&gt;.item} 与 foreach[&quot;&lt;Id&gt;&quot;],下标写入 ${$foreach.&lt;Id&gt;.index}。"
                                               FontSize="10" Opacity="0.55" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- EndForEach: close a foreach body -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsEndForEachKind}" Margin="0,4,0,0">
                                    <TextBlock Text="遍历结束:" FontSize="11"
                                               Foreground="#94E2D5" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="ForEachId:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox Text="{Binding SelectedStep.ForEachId}"
                                                 Width="100" Padding="4,2"
                                                 FontFamily="Consolas,monospace"
                                                 VerticalContentAlignment="Center"
                                                 ToolTip.Tip="必须与匹配的 ForEach 步骤相同"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- CallScenario: invoke another scenario as sub-routine -->
                                <StackPanel IsVisible="{Binding SelectedStep.IsCallScenarioKind}" Margin="0,4,0,0">
                                    <TextBlock Text="调用子场景:" FontSize="11"
                                               Foreground="#CBA6F7" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="场景名:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <AutoCompleteBox Classes="templatePicker"
                                                         ItemsSource="{Binding ScenarioNames}"
                                                         Text="{Binding SelectedStep.SubScenarioName}"
                                                         FilterMode="Contains"
                                                         Padding="4,2" FontSize="11" MinWidth="240"
                                                         Watermark="(输入关键字过滤场景)"/>
                                    </StackPanel>
                                    <TextBlock Text="提示: 变量在父子场景间共享;最大递归深度 16。"
                                               FontSize="10" Opacity="0.55" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Empty state -->
                        <TextBlock x:Name="StepEmptyState"
                                   Text="← 选择左侧场景和步骤进行编辑"
                                   Opacity="0.4" FontSize="12"
                                   HorizontalAlignment="Center"
                                   Margin="0,40,0,0"/>

                        <!-- Debugger: variable watch panel — visible only while paused. -->
                        <Border Background="#1F1F1F" CornerRadius="4" Padding="8,6" Margin="0,12,0,0"
                                IsVisible="{Binding IsPaused}">
                            <StackPanel>
                                <TextBlock Text="变量观察 (Paused)" FontSize="11" FontWeight="SemiBold"
                                           Foreground="#FFB454" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding PausedStepIndex, StringFormat='当前停在步骤 #{0}'}"
                                           FontSize="10" Opacity="0.7" Margin="0,0,0,6"/>
                                <ItemsControl ItemsSource="{Binding WatchVariables}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,1">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="2*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding Name}"
                                                           FontFamily="Consolas,monospace" FontSize="11"
                                                           Foreground="#A6E3A1"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Value}"
                                                           FontFamily="Consolas,monospace" FontSize="11"
                                                           Foreground="#CDD6F4"
                                                           TextTrimming="CharacterEllipsis"
                                                           Margin="8,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </DockPanel>
</UserControl>
